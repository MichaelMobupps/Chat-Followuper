import { PagePlaceholder } from "@/components/page-placeholder";

export default function ActivityPage() {
  return (
    <PagePlaceholder
      title="Activity"
      description="Recent system events, sends, and reply activity."
    />
  );
}
